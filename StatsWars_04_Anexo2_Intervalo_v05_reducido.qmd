---
title: "Anexo X: Intervalo de conflictos"
format:
  docx:
    reference-doc: Res CS 2_23 - Carátula de TF UNTREF.docx
execute:
  warning: false
  message: false
  error: false
  echo: false
  engine: r
---

```{r}
#| include: false

library(tidyverse)
library(readr)
library(lubridate)
library(survival)
library(survminer)
library(broom)
library(knitr)
library(gt)
library(patchwork)

theme_set(theme_minimal())

# Función para asegurar 300 dpi en todas las figuras
knitr::opts_chunk$set(
  fig.dpi = 300,
  fig.width = 5, 
  fig.height = 3
)

# Input ------------------------------------------------------------------
anu_frec_cow <- readRDS("data_gold/anu_frec_cow.rds")
anu_frec_sdq <- readRDS("data_gold/anu_frec_sdq.rds")
anu_frec_ucdp <- readRDS("data_gold/anu_frec_ucdp.rds")

conf_durint_cow <- readRDS("data_gold/conf_durint_cow.rds")
conf_durint_sdq <- readRDS("data_gold/conf_durint_sdq.rds")
conf_durint_ucdp <- readRDS("data_gold/conf_durint_ucdp.rds")

time_between_cow <- readRDS("data_gold/time_between_cow.rds")
time_between_sdq <- readRDS("data_gold/time_between_sdq.rds")
time_between_ucdp <- readRDS("data_gold/time_between_ucdp.rds")
```

# Análisis Exploratorio de Datos

## Resumen numérico

```{r}
list(
  SDQ = summary(time_between_sdq$time_between),
  COW = summary(time_between_cow$time_between),
  UCDP = summary(time_between_ucdp$time_between)
)
```

## Distribución: histogramas comparado

```{r}
#| fig.height: 7
#| fig.width: 5

p_sdq <- ggplot(time_between_sdq, aes(time_between)) +
  geom_histogram(bins = 30, fill = "steelblue") +
  labs(
    x = "Intervalos (meses)",
    y = "Conteo - SDQ"
    ) +
        theme_minimal()

p_cow <- ggplot(time_between_cow, aes(time_between)) +
  geom_histogram(bins = 30, fill = "firebrick") +
  labs(
    x = "Intervalos (meses)",
    y = "Conteo - COW"
    ) +
        theme_minimal()

p_ucdp <- ggplot(time_between_ucdp, aes(time_between)) +
  geom_histogram(bins = 30, fill = "darkgreen") +
  labs(
    x = "Intervalos (meses)",
    y = "Conteo - UCDP"
    ) +
        theme_minimal()

p_sdq / p_cow / p_ucdp

```

## Boxplots para detectar outlier

```{r}
bind_rows(
  time_between_sdq |> mutate(dataset = "SDQ"),
  time_between_cow |> mutate(dataset = "COW"),
  time_between_ucdp |> mutate(dataset = "UCDP")
) |>
  ggplot(aes(x = dataset, y = time_between, fill = dataset)) +
  geom_boxplot() +
  scale_y_log10() +
  labs(
    y = "Intervalos (escala log 10)",
    x = "Dataset"
    ) +
        theme_minimal()
# Usamos log10 porque los intervalos pueden ser enormes.
```

## Tendencia temporal

```{r}
#| fig.height: 7
#| fig.width: 5

p_sdq <- ggplot(time_between_sdq, aes(year, time_between)) +
  geom_point(alpha = 0.5) + geom_smooth() +
  labs(x = "Año", y = "Intervalo (meses) - SDQ") + theme_minimal()

p_cow <- ggplot(time_between_cow, aes(year, time_between)) +
  geom_point(alpha = 0.5) + geom_smooth() +
  labs(x = "Año", y = "Intervalo (meses) - COW") + theme_minimal()

p_ucdp <- ggplot(time_between_ucdp, aes(year, time_between)) +
  geom_point(alpha = 0.5) + geom_smooth() +
  labs(x = "Año", y = "Intervalo (meses) - UCDP") + theme_minimal()

p_sdq / p_cow / p_ucdp

```

## Comparación por fase

```{r}
time_between_sdq |>
  filter(!is.na(phase)) |> 
  # filter(time_between < quantile(time_between, 0.90, na.rm = TRUE)) |>
  ggplot(aes(phase, time_between)) +
  geom_boxplot() +
  scale_y_log10() +
  labs(
    x = "Fase",
    y = "Intervalo (meses escala log) - SDQ",
    ) + 
    theme_minimal() +
    coord_flip()

time_between_cow |>
  filter(!is.na(phase)) |>
  ggplot(aes(phase, time_between)) +
  geom_boxplot() +
  scale_y_log10() +
  labs(
    x = "Fase",
    y = "Intervalo (meses escala log) - COW",
    ) + 
    theme_minimal() +
    coord_flip()

time_between_ucdp |>
  filter(!is.na(phase)) |>
  ggplot(aes(phase, time_between)) +
  geom_boxplot() +
  scale_y_log10() +
  labs(
    x = "Fase",
    y = "Intervalo (meses escala log) - UCDP",
    ) + 
    theme_minimal() +
    coord_flip()
```

## Comparación por Rimland (solo COW y UCDP)

```{r}
time_between_cow |>
  ggplot(aes(factor(rimland), time_between)) +
  geom_boxplot() +
  scale_y_log10() +
  labs(
    x = "Rimland (1=Sí, 0=No)",
    y = "Intervalo (meses escala log) - COW"
    ) +
    theme_minimal()

time_between_ucdp |>
  ggplot(aes(factor(rimland), time_between)) +
  geom_boxplot() +
  scale_y_log10() +
  labs(
    x = "Rimland (1=Sí, 0=No)",
    y = "Intervalo (meses escala log) - UCDP"
    ) +
    theme_minimal()
```

## Comparación por shock

```{r}
time_between_sdq |>
  select(time_between, shock_1939, shock_1945) |>
  pivot_longer(starts_with("shock_"), names_to = "shock", values_to = "valor") |>
  ggplot(aes(factor(valor), time_between)) +
  geom_boxplot() +
  scale_y_log10() +
  facet_wrap(~ shock) +
  labs(x = "Pre (0) / Post (1)", y = "Intervalo (meses log) - SDQ") +
  theme_minimal()

time_between_cow |>
  select(time_between, shock_1939, shock_1945) |>
  pivot_longer(starts_with("shock_"), names_to = "shock", values_to = "valor") |>
  ggplot(aes(factor(valor), time_between)) +
  geom_boxplot() +
  scale_y_log10() +
  facet_wrap(~ shock) +
  labs(x = "Pre (0) / Post (1)", y = "Intervalo (meses log) - COW") +
  theme_minimal()

time_between_ucdp |>
  select(time_between, shock_1945, shock_1991) |>
  pivot_longer(starts_with("shock_"), names_to = "shock", values_to = "valor") |>
  ggplot(aes(factor(valor), time_between)) +
  geom_boxplot() +
  scale_y_log10() +
  facet_wrap(~ shock) +
  labs(x = "Pre (0) / Post (1)", y = "Intervalo (meses log) - UCDP") +
  theme_minimal()
```

## Correlación simple con año

```{r}
cor.test(time_between_sdq$time_between, time_between_sdq$year)
# se acelera
cor.test(time_between_cow$time_between, time_between_cow$year)
# no significativo
cor.test(time_between_ucdp$time_between, time_between_ucdp$year)
# no significativo
```

# Revisión de supuestos para el modelo Exponencial
```{r}
#| echo: false
sdq <- time_between_sdq |>
  filter(!is.na(time_between)) |>
  mutate(
    phase = recode(
      phase,
      "unipolaridad_tardia_multipolaridad_emergente" = "unipol_tard",
      "unipolaridad_temprana" = "unipol_temp",
      "multipolaridad_clasica" = "multipol_cl",
      "entreguerras" = "entreg",
      "ruptura_sistema" = "rupt_sist",
      "bipolaridad" = "bipolar"
    )
  ) |>
  mutate(
    time_between = case_when(
      time_between == 0 ~ 0.5,
      TRUE ~ time_between
    ),
    phase = factor(phase),
    shock_1939 = factor(shock_1939),
    shock_1945 = factor(shock_1945)
  )

cow <- time_between_cow |>
  filter(!is.na(time_between)) |>
  mutate(
    phase = recode(
      phase,
      "unipolaridad_tardia_multipolaridad_emergente" = "unipol_tard",
      "unipolaridad_temprana" = "unipol_temp",
      "multipolaridad_clasica" = "multipol_cl",
      "entreguerras" = "entreg",
      "ruptura_sistema" = "rupt_sist",
      "bipolaridad" = "bipolar"
    )
  ) |>
  mutate(
    time_between = case_when(
      time_between == 0 ~ 0.5,
      TRUE ~ time_between
    ),
    phase = factor(phase),
    shock_1939 = factor(shock_1939),
    shock_1945 = factor(shock_1945),
    rimland = factor(rimland)
  )

ucdp <- time_between_ucdp |>
  filter(!is.na(time_between)) |>
  mutate(
    phase = recode(
      phase,
      "unipolaridad_tardia_multipolaridad_emergente" = "unipol_tard",
      "unipolaridad_temprana" = "unipol_temp",
      "multipolaridad_clasica" = "multipol_cl",
      "entreguerras" = "entreg",
      "ruptura_sistema" = "rupt_sist",
      "bipolaridad" = "bipolar"
    )
  ) |>
  mutate(
    time_between = case_when(
      time_between == 0 ~ 0.5,
      TRUE ~ time_between
    ),
    phase = factor(phase),
    shock_1945 = factor(shock_1945),
    shock_1991 = factor(shock_1991),
    rimland = factor(rimland)
  )

```

## Correlación año–intervalo (esperado ≈ 0 si hazard constante)

La magnitud de las correlaciones es muy baja en los tres datasets, lo que respalda el supuesto de hazard constante.

```{r}
cor(sdq$year, sdq$time_between, use = "complete.obs")
cor(cow$year, cow$time_between, use = "complete.obs")
cor(ucdp$year, ucdp$time_between, use = "complete.obs")
```

## Comparación exponencial vs Weibull (shape ≈ 1)

Se estimaron modelos Exponencial y Weibull base para evaluar si el supuesto de hazard constante es suficiente; el test LR y el AIC determinan la especificación preferida por dataset.

```{r}
# SDQ
exp_sdq <- survreg(Surv(time_between) ~ 1, data = sdq, dist = "exponential")
wei_sdq <- survreg(Surv(time_between) ~ 1, data = sdq, dist = "weibull")

# COW
exp_cow <- survreg(Surv(time_between) ~ 1, data = cow, dist = "exponential")
wei_cow <- survreg(Surv(time_between) ~ 1, data = cow, dist = "weibull")

# UCDP
exp_ucdp <- survreg(Surv(time_between) ~ 1, data = ucdp, dist = "exponential")
wei_ucdp <- survreg(Surv(time_between) ~ 1, data = ucdp, dist = "weibull")

summary(wei_sdq)
summary(wei_cow)
summary(wei_ucdp)
```


```{r}
shape_sdq <- 1 / wei_sdq$scale
shape_cow <- 1 / wei_cow$scale
shape_ucdp <- 1 / wei_ucdp$scale

shape_sdq
shape_cow
shape_ucdp
```

## Test de razón de verosimilitud (LR test)

```{r}
# SDQ
lr_sdq <- 2 * (logLik(wei_sdq) - logLik(exp_sdq))
pchisq(lr_sdq, df = 1, lower.tail = FALSE)

# COW
lr_cow <- 2 * (logLik(wei_cow) - logLik(exp_cow))
pchisq(lr_cow, df = 1, lower.tail = FALSE)

# UCDP
lr_ucdp <- 2 * (logLik(wei_ucdp) - logLik(exp_ucdp))
pchisq(lr_ucdp, df = 1, lower.tail = FALSE)
```


## Comparación AIC

```{r}
AIC(exp_sdq, wei_sdq)
AIC(exp_cow, wei_cow)
AIC(exp_ucdp, wei_ucdp)
# Si AIC(exponencial) ≈ AIC(weibull) → exponencial es suficiente

```

## Curvas de supervivencia empírica vs exponencial teórica

Las curvas empíricas confirman que la estructura general de los datos es compatible con modelos de supervivencia. 

```{r}
# SDQ
fit_sdq <- survfit(Surv(time_between) ~ 1, data = sdq)
ggsurvplot(fit_sdq) + ggtitle("SDQ — Curva empírica de supervivencia")

# COW
fit_cow <- survfit(Surv(time_between) ~ 1, data = cow)
ggsurvplot(fit_cow) + ggtitle("COW — Curva empírica de supervivencia")

# UCDP
fit_ucdp <- survfit(Surv(time_between) ~ 1, data = ucdp)
ggsurvplot(fit_ucdp) + ggtitle("UCDP — Curva empírica de supervivencia")

```

## Gráfico log(-log(S(t))) vs log(t)

La inclinación de la curva indica si la Weibull supera a la Exponencial: horizontal = Exponencial suficiente; recta inclinada = Weibull preferida.

```{r}
# Este es el test visual clásico para Weibull:
# - Si es línea recta → Weibull
# - Si es curva horizontal → Exponencial (shape = 1)

plot(fit_sdq, fun = "cloglog", main = "SDQ — log(-log(S)) vs log(t)")
plot(fit_cow, fun = "cloglog", main = "COW — log(-log(S)) vs log(t)")
plot(fit_ucdp, fun = "cloglog", main = "UCDP — log(-log(S)) vs log(t)")
```

# Modelos estadísticos

## SDQ

### Modelo base (Exponencial)

```{r}
m_sdq_base <- survreg(
  Surv(time_between) ~ 1,
  data = sdq,
  dist = "exponential"
)

broom::tidy(m_sdq_base, conf.int = TRUE) |>
  select(term, estimate, p.value, conf.low, conf.high) |>
  mutate(
    TR = exp(estimate),
    HR = exp(-estimate),
    conf.low.TR = exp(conf.low),
    conf.high.TR = exp(conf.high)
  ) |>
  select(term, estimate, TR, HR, p.value, conf.low.TR, conf.high.TR) |>
  knitr::kable(digits = 3)
# - El modelo predice tiempo esperado entre eventos, no hazard directamente
# - El coeficiente β afecta el tiempo esperado multiplicándolo por exp(β)
# - El hazard ratio se obtiene como: HR = exp(−β)
# Porque hazard = 1 / tiempo esperado.
# Entonces:
# - β > 0 → tiempo esperado ↑ → hazard ↓
# - β < 0 → tiempo esperado ↓ → hazard ↑
# Y el hazard ratio te dice:
# “Cuántas veces más (o menos) probable es que ocurra el próximo conflicto en un momento dado.”
```

### Modelo A — Fases del sistema internacional

```{r}
# Exponencial
m_sdq_phase <- survreg(
  Surv(time_between) ~ phase,
  data = sdq,
  dist = "exponential"
)
broom::tidy(m_sdq_phase, conf.int = TRUE) |>
  select(term, estimate, p.value, conf.low, conf.high) |>
  mutate(
    TR = exp(estimate),
    HR = exp(-estimate),
    conf.low.TR = exp(conf.low),
    conf.high.TR = exp(conf.high)
  ) |>
  select(term, estimate, TR, HR, p.value, conf.low.TR, conf.high.TR) |>
  knitr::kable(digits = 3)
```

```{r}
# Weibull
m_sdq_phase_wei <- survreg(
  Surv(time_between) ~ phase,
  data = sdq,
  dist = "weibull"
)
broom::tidy(m_sdq_phase_wei, conf.int = TRUE) |>
  select(term, estimate, p.value, conf.low, conf.high) |>
  mutate(
    TR = exp(estimate),
    HR = exp(-estimate),
    conf.low.TR = exp(conf.low),
    conf.high.TR = exp(conf.high)
  ) |>
  select(term, estimate, TR, HR, p.value, conf.low.TR, conf.high.TR) |>
  knitr::kable(digits = 3)
```

### Modelo B — Shocks históricos (1939 y 1945)

```{r}
# Shock 1939
m_sdq_1939 <- survreg(
  Surv(time_between) ~ shock_1939,
  data = sdq,
  dist = "exponential"
)
broom::tidy(m_sdq_1939, conf.int = TRUE) |>
  select(term, estimate, p.value, conf.low, conf.high) |>
  mutate(
    TR = exp(estimate),
    HR = exp(-estimate),
    conf.low.TR = exp(conf.low),
    conf.high.TR = exp(conf.high)
  ) |>
  select(term, estimate, TR, HR, p.value, conf.low.TR, conf.high.TR) |>
  knitr::kable(digits = 3)
# Significativo.

# Shock 1945
m_sdq_1945 <- survreg(
  Surv(time_between) ~ shock_1945,
  data = sdq,
  dist = "exponential"
)
broom::tidy(m_sdq_1945, conf.int = TRUE) |>
  select(term, estimate, p.value, conf.low, conf.high) |>
  mutate(
    TR = exp(estimate),
    HR = exp(-estimate),
    conf.low.TR = exp(conf.low),
    conf.high.TR = exp(conf.high)
  ) |>
  select(term, estimate, TR, HR, p.value, conf.low.TR, conf.high.TR) |>
  knitr::kable(digits = 3)
# Significativo.
```

```{r}
# Shock 1939
m_sdq_1939_wei <- survreg(
  Surv(time_between) ~ shock_1939,
  data = sdq,
  dist = "weibull"
)
broom::tidy(m_sdq_1939_wei, conf.int = TRUE) |>
  select(term, estimate, p.value, conf.low, conf.high) |>
  mutate(
    TR = exp(estimate),
    HR = exp(-estimate),
    conf.low.TR = exp(conf.low),
    conf.high.TR = exp(conf.high)
  ) |>
  select(term, estimate, TR, HR, p.value, conf.low.TR, conf.high.TR) |>
  knitr::kable(digits = 3)
# .

# Shock 1945
m_sdq_1945_wei <- survreg(
  Surv(time_between) ~ shock_1945,
  data = sdq,
  dist = "weibull"
)
broom::tidy(m_sdq_1945_wei, conf.int = TRUE) |>
  select(term, estimate, p.value, conf.low, conf.high) |>
  mutate(
    TR = exp(estimate),
    HR = exp(-estimate),
    conf.low.TR = exp(conf.low),
    conf.high.TR = exp(conf.high)
  ) |>
  select(term, estimate, TR, HR, p.value, conf.low.TR, conf.high.TR) |>
  knitr::kable(digits = 3)
# .
```

### Modelo C — Modelo completo (fases + shock 1945)

```{r}
m_sdq_full <- survreg(
  Surv(time_between) ~ phase + shock_1945,
  data = sdq,
  dist = "exponential"
)
broom::tidy(m_sdq_full, conf.int = TRUE) |>
  select(term, estimate, p.value, conf.low, conf.high) |>
  mutate(
    TR = exp(estimate),
    HR = exp(-estimate),
    conf.low.TR = exp(conf.low),
    conf.high.TR = exp(conf.high)
  ) |>
  select(term, estimate, TR, HR, p.value, conf.low.TR, conf.high.TR) |>
  knitr::kable(digits = 3)
# No significativo
```

```{r}
m_sdq_weibull <- survreg(
  Surv(time_between) ~ phase + shock_1945,
  data = sdq,
  dist = "weibull"
)

broom::tidy(m_sdq_weibull, conf.int = TRUE) |>
  select(term, estimate, p.value, conf.low, conf.high) |>
  mutate(
    TR = exp(estimate),
    HR = exp(-estimate),
    conf.low.TR = exp(conf.low),
    conf.high.TR = exp(conf.high)
  ) |>
  select(term, estimate, TR, HR, p.value, conf.low.TR, conf.high.TR) |>
  knitr::kable(digits = 3)
# AIC(m_sdq_base, m_sdq_phase, m_sdq_1939, m_sdq_1945, m_sdq_full, m_sdq_weibull)

lr_sdq <- 2 * (logLik(m_sdq_weibull) - logLik(m_sdq_full))
pchisq(lr_sdq, df = 1, lower.tail = FALSE)
```

### Comparación de modelos

```{r}
AIC(
    m_sdq_base, 
    m_sdq_phase,
    m_sdq_phase_wei, 
    m_sdq_1939,
    m_sdq_1939_wei,
    m_sdq_1945,
    m_sdq_1945_wei,
    m_sdq_full, 
    m_sdq_weibull
    )

```

## COW

### Modelo base (Exponencial)

```{r}

m_cow_base <- survreg(
  Surv(time_between) ~ 1,
  data = cow,
  dist = "exponential"
)
broom::tidy(m_cow_base, conf.int = TRUE) |>
  select(term, estimate, p.value, conf.low, conf.high) |>
  mutate(
    TR = exp(estimate),
    HR = exp(-estimate),
    conf.low.TR = exp(conf.low),
    conf.high.TR = exp(conf.high)
  ) |>
  select(term, estimate, TR, HR, p.value, conf.low.TR, conf.high.TR) |>
  knitr::kable(digits = 3)
```

### Modelo A — Fases del sistema internacional

```{r}
# Exponencial
m_cow_phase <- survreg(
  Surv(time_between) ~ phase,
  data = cow,
  dist = "exponential"
)
broom::tidy(m_cow_phase, conf.int = TRUE) |>
  select(term, estimate, p.value, conf.low, conf.high) |>
  mutate(
    TR = exp(estimate),
    HR = exp(-estimate),
    conf.low.TR = exp(conf.low),
    conf.high.TR = exp(conf.high)
  ) |>
  select(term, estimate, TR, HR, p.value, conf.low.TR, conf.high.TR) |>
  knitr::kable(digits = 3)
```

### Modelo B — Shocks históricos (1939 y 1945)

```{r}
# Shock 1939
m_cow_1939 <- survreg(
  Surv(time_between) ~ shock_1939,
  data = cow,
  dist = "exponential"
)
broom::tidy(m_cow_1939, conf.int = TRUE) |>
  select(term, estimate, p.value, conf.low, conf.high) |>
  mutate(
    TR = exp(estimate),
    HR = exp(-estimate),
    conf.low.TR = exp(conf.low),
    conf.high.TR = exp(conf.high)
  ) |>
  select(term, estimate, TR, HR, p.value, conf.low.TR, conf.high.TR) |>
  knitr::kable(digits = 3)
# No significativo.

# Shock 1945
m_cow_1945 <- survreg(
  Surv(time_between) ~ shock_1945,
  data = cow,
  dist = "exponential"
)
broom::tidy(m_cow_1945, conf.int = TRUE) |>
  select(term, estimate, p.value, conf.low, conf.high) |>
  mutate(
    TR = exp(estimate),
    HR = exp(-estimate),
    conf.low.TR = exp(conf.low),
    conf.high.TR = exp(conf.high)
  ) |>
  select(term, estimate, TR, HR, p.value, conf.low.TR, conf.high.TR) |>
  knitr::kable(digits = 3)
# No significativo.
```

### Modelo C — Rimland

```{r}
# Explonencial
m_cow_rimland <- survreg(
  Surv(time_between) ~ rimland,
  data = cow,
  dist = "exponential"
)
broom::tidy(m_cow_rimland, conf.int = TRUE) |>
  select(term, estimate, p.value, conf.low, conf.high) |>
  mutate(
    TR = exp(estimate),
    HR = exp(-estimate),
    conf.low.TR = exp(conf.low),
    conf.high.TR = exp(conf.high)
  ) |>
  select(term, estimate, TR, HR, p.value, conf.low.TR, conf.high.TR) |>
  knitr::kable(digits = 3)
```

### Modelo D — Modelo completo (fases + Rimland + shock 1945)

```{r}
m_cow_full <- survreg(
  Surv(time_between) ~ phase + rimland + shock_1945,
  data = cow,
  dist = "exponential"
)
broom::tidy(m_cow_full, conf.int = TRUE) |>
  select(term, estimate, p.value, conf.low, conf.high) |>
  mutate(
    TR = exp(estimate),
    HR = exp(-estimate),
    conf.low.TR = exp(conf.low),
    conf.high.TR = exp(conf.high)
  ) |>
  select(term, estimate, TR, HR, p.value, conf.low.TR, conf.high.TR) |>
  knitr::kable(digits = 3)

```

### Comparación de modelos

```{r}
AIC(
    m_cow_base, 
    m_cow_phase, 
    m_cow_rimland, 
    m_cow_1939, 
    m_cow_1945, 
    m_cow_full
    )

```

## UCDP

### Modelo base (Exponencial)

```{r}
# Exponencial
m_ucdp_base <- survreg(
  Surv(time_between) ~ 1,
  data = ucdp,
  dist = "exponential"
)
broom::tidy(m_ucdp_base, conf.int = TRUE) |>
  select(term, estimate, p.value, conf.low, conf.high) |>
  mutate(
    TR = exp(estimate),
    HR = exp(-estimate),
    conf.low.TR = exp(conf.low),
    conf.high.TR = exp(conf.high)
  ) |>
  select(term, estimate, TR, HR, p.value, conf.low.TR, conf.high.TR) |>
  knitr::kable(digits = 3)

```

### Modelo A — Fases del sistema internacional

```{r}
# Exponencial
m_ucdp_phase <- survreg(
  Surv(time_between) ~ phase,
  data = ucdp,
  dist = "exponential"
)
broom::tidy(m_ucdp_phase, conf.int = TRUE) |>
  select(term, estimate, p.value, conf.low, conf.high) |>
  mutate(
    TR = exp(estimate),
    HR = exp(-estimate),
    conf.low.TR = exp(conf.low),
    conf.high.TR = exp(conf.high)
  ) |>
  select(term, estimate, TR, HR, p.value, conf.low.TR, conf.high.TR) |>
  knitr::kable(digits = 3)
```

### Modelo B — Shocks históricos (1945 y 1991)

```{r}
# Exponencial
m_ucdp_1945 <- survreg(
  Surv(time_between) ~ shock_1945,
  data = ucdp,
  dist = "exponential"
)
broom::tidy(m_ucdp_1945, conf.int = TRUE) |>
  select(term, estimate, p.value, conf.low, conf.high) |>
  mutate(
    TR = exp(estimate),
    HR = exp(-estimate),
    conf.low.TR = exp(conf.low),
    conf.high.TR = exp(conf.high)
  ) |>
  select(term, estimate, TR, HR, p.value, conf.low.TR, conf.high.TR) |>
  knitr::kable(digits = 3)
```

```{r}
# Exponencial
m_ucdp_1991 <- survreg(
  Surv(time_between) ~ shock_1991,
  data = ucdp,
  dist = "exponential"
)
broom::tidy(m_ucdp_1991, conf.int = TRUE) |>
  select(term, estimate, p.value, conf.low, conf.high) |>
  mutate(
    TR = exp(estimate),
    HR = exp(-estimate),
    conf.low.TR = exp(conf.low),
    conf.high.TR = exp(conf.high)
  ) |>
  select(term, estimate, TR, HR, p.value, conf.low.TR, conf.high.TR) |>
  knitr::kable(digits = 3)
# No significativo.
```

### Modelo C — Rimland

```{r}
# Exponencial
m_ucdp_rimland <- survreg(
  Surv(time_between) ~ rimland,
  data = ucdp,
  dist = "exponential"
)
broom::tidy(m_ucdp_rimland, conf.int = TRUE) |>
  select(term, estimate, p.value, conf.low, conf.high) |>
  mutate(
    TR = exp(estimate),
    HR = exp(-estimate),
    conf.low.TR = exp(conf.low),
    conf.high.TR = exp(conf.high)
  ) |>
  select(term, estimate, TR, HR, p.value, conf.low.TR, conf.high.TR) |>
  knitr::kable(digits = 3)
```

### Modelo D — Modelo completo (fases + Rimland + shocks)

```{r}
m_ucdp_full <- survreg(
  Surv(time_between) ~ phase + rimland + shock_1945 + shock_1991,
  data = ucdp,
  dist = "exponential"
)
broom::tidy(m_ucdp_full, conf.int = TRUE) |>
  select(term, estimate, p.value, conf.low, conf.high) |>
  mutate(
    TR = exp(estimate),
    HR = exp(-estimate),
    conf.low.TR = exp(conf.low),
    conf.high.TR = exp(conf.high)
  ) |>
  select(term, estimate, TR, HR, p.value, conf.low.TR, conf.high.TR) |>
  knitr::kable(digits = 3)
```

```{r}
AIC(
  m_ucdp_base,
  m_ucdp_phase,
  m_ucdp_rimland,
  m_ucdp_1945,
  m_ucdp_1991,
  m_ucdp_full
)
```
